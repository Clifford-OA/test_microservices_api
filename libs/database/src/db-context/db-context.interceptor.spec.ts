import { DbContextInterceptor } from './db-context.interceptor';

describe('DbContextInterceptor', () => {
  it('should be defined', () => {
    expect(new DbContextInterceptor()).toBeDefined();
  });
});
